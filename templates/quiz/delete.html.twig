{% include 'default/_form.html.twig' with {
    page_title: 'title.quiz_delete'|trans({'%id%': quiz.id|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'quiz_index',
} %}