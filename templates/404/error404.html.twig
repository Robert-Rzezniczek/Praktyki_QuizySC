{% extends 'base.html.twig' %}

{% block title %}{{ 'label.page_not_found'|trans }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href="https://fonts.googleapis.com/css2?family=Luckiest+Guy&display=swap" rel="stylesheet">
    <style>
        .error-container {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            min-height: 75vh;
            text-align: center;
            padding: 10px;
        }

        .error-title {
            font-size: 3rem;
            color: #dc3545;
            margin-bottom: 1rem;
            font-family: 'Luckiest Guy', cursive;
        }

        .error-text {
            font-size: 1.25rem;
            color: #6c757d;
            margin-bottom: 1rem;
        }

        .error-emoji {
            font-size: 4rem;
            margin-bottom: 1rem;
            font-family: 'Luckiest Guy', cursive;
        }

        .thinking-emoji {
            font-size: 6rem;
            margin-top: 1rem;
            margin-bottom: 1.5rem;
        }

        .error-button {
            background-color: #6a1b9a;
            color: white;
            padding: 20px 50px;
            font-size: 1.6rem;
            border-radius: 50px;
            text-decoration: none;
            transition: transform 0.2s ease;
            display: inline-block;
            font-family: 'Luckiest Guy', cursive;
        }

        .error-button:hover {
            background-color: #6a1b9a;
            filter: brightness(1.1);
            transform: scale(1.02);
        }

        @media (max-width: 768px) {
            .error-title {
                font-size: 2.5rem;
            }
            .error-text {
                font-size: 1rem;
            }
            .error-emoji {
                font-size: 3rem;
            }
            .thinking-emoji {
                font-size: 4rem;
            }
            .error-button {
                font-size: 1rem;
                padding: 8px 16px;
            }
        }
    </style>
{% endblock %}

{% block body %}
    <div class="error-container">
        <div class="error-emoji">Ups!</div>
        <h1 class="error-title">404 – Pytanie bez odpowiedzi!</h1>
        <p class="error-text">Wygląda na to, że ta strona nie istnieje...</p>
        <p class="error-text">Ale za to istnieje mnóstwo świetnych quizów!</p>
        <div class="thinking-emoji">🤔</div>
        <a href="{{ path('app_menu') }}" class="error-button mt-3">
            Powrót do MENU
        </a>
    </div>
{% endblock %}